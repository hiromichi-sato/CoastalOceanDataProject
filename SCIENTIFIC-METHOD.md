# 実観測解析の仕様

## 利用可能なデータ

起動画面 `/` は実データ専用です。環境省 ArcGIS、Argo Ifremer ERDDAP、CCHDO NOAA PMEL ERDDAPからサーバーが観測値を取得します。利用者が ZIP や CSV を先に保存する必要はありません。通信によるデータ取得自体は必要です。提供元の停止・欠測をサンプルで置き換えることはありません。

- 広域総合: 東京湾・伊勢湾・大阪湾・瀬戸内海。観測年月・採水深度による選択。
- 公共用水域: 公開 GIS の年間値。年度・全月のときのみ採用し、深度指定は適用不能として警告します。矩形の検索範囲には湾外の測点が含まれる可能性があります。
- Argo: IfremerのArgoFloatsを実接続。水温・実用塩分・観測海圧・換算深度・EOS-80密度に対応。BGCの酸素・栄養塩・pHは未対応。
- CCHDO / GO-SHIP: 船舶CTDを実接続。水温・実用塩分・海圧・換算深度・密度・酸素に対応。JAMSTEC DARWINや気象庁の全航海を直接取得するものではなく、CCHDOへの収録分のみ対象です。
- 25項目を提供: 水温、実用塩分、密度2種、σt、推定海圧、採水深度、測点水深、pH、DO、COD、溶存態COD、透明度、TN、TP、NH4-N、NO2-N、NO3-N、PO4-P、クロロフィルa、フェオフィチン、TOC、DOC、BOD、SS。提供元にない項目・年代・海域の値は作りません。海域のBODなどは観測値が得られない場合があります。

出典: [広域総合 GIS](https://water-pub.env.go.jp/gis/rest/services/KOIKI_01/MapServer/0)、[公共用水域ダウンロード](https://water-pub.env.go.jp/water-pub/mizu-site/mizu/download/download.asp)。出力には取得日時・問い合わせ条件・サービスURLを記録します。ページ分割して取得し、重複IDや10万件超過はエラーとします。取得結果のメモリーキャッシュは1時間です。

## 時間・深度・集約

### 外部プロファイル

[Argoテーブル](https://erddap.ifremer.fr/erddap/tabledap/ArgoFloats.html)、[CCHDO CTDテーブル](https://data.pmel.noaa.gov/generic/erddap/tabledap/cchdo_ctd.html)を年ごとに条件取得します。日本近海（120–155°E、20–47°N）のプリセット／矩形指定に対応。日本のみの海岸線マスクのため、国外陸域の遮断は保証されません。

- 1回10年以内・100平方度以内・深度幅500m以内。1応答20MB、全体15万行まで。超過は切り捨てずエラー。提供元エラーは明示し、別ソースの成功分を表示する場合も部分結果と表示・保存します。
- Argoは既定で調整済みA/Dモード、品質フラグ1・2のみ。位置・時刻・圧力もQC対象。Dのみ、またはRを含める設定を選択できます。A/Dで調整値が欠測・QC不良なら、生の値へ戻して採用しません。
- CCHDOはWOCE QC=2のみ。ITS-90を優先し、なければ良好なITS-68を1.00024で除して変換。温度スケール不明の値は除外。酸素はμmol/kgをEOS-80密度でmg/Lに換算するため、良好な水温・塩分も必要です。圧力の独立QC列はなく有限性・範囲をチェックします。
- 深度は観測海圧と緯度からEOS-80で換算。圧力抽出に余裕を持たせてから指定深度で選別し、両端を含めます。
- 同一プロファイル・同一圧力の重複を除去。深度内の有効層を算術平均して1プロファイル1標本とし、経緯度区画（既定0.25°）で年月集約します。鉛直積分平均ではありません。移動するフロート全体を固定測点にはしません。
- 区画は経緯度0°を原点とする半開区間（floor(coordinate / binWidth)）。表示位置は有効プロファイルの平均位置です。区画内で陸地を挟む観測の混合や観測密度の偏りは残るため、ソース別表示も確認してください。
- 外部時刻の暦年・年度・月判定はUTCです。国内沿岸観測の月境界の時間帯とは完全には一致しない場合があります。
- 保存データにモード、QC方針、実取得URL、日時、航海リンク、WMO/EXPocode、空間集約幅を記録します。[Argo DOI](https://doi.org/10.17882/42182)。研究利用・引用は提供元の条件に従ってください。

### 共通処理

- 開始年・終了年は両端を含みます。暦年は `surveyyear`、年度は `nendo` を使います。
- 季節は春3–5月、夏6–8月、秋9–11月、冬12・1・2月。指定月と季節の共通部分を採用します。冬は選択した暦年／年度内の該当月であり、12月を翌年の冬へ移すDJF年ではありません。
- 採水深度の上下限は両端を含みます。同じ測点の選択深度内に複数観測がある場合、それぞれを観測として平均します。密度・水温などの鉛直断面や等密度面の解析ではありません。
- 月・年均等: 測点ごとに観測→年月平均→年平均→期間平均。取得頻度の多い月・年だけに重みが偏らない方式です。水域グラフは測点を均等平均します。面積平均・体積平均ではありません。
- 観測件数加重: 測点内の全観測の算術平均。水域グラフも観測件数で加重します。
- 欠測年月は分母から除外します。完全な9年分などを保証せず、有効年・観測数・欠測年を併記します。季節比較では観測頻度・測点配置の変化に注意してください。
- 空文字・null・欠測符号を0に変換しません。定量下限未満は既定で除外、選択時のみ下限の1/2とします。提供元の全品質管理フラグを解釈する処理や外れ値の高度なQCは未実装です。
- ソース統合は同一の単位・項目の測点値をまとめます。機関間の重複観測の照合・測定方法差の補正は行いません。ソース別表示も選択できます。

## 拡散方程式

一定の正の拡散係数 κ に対して `∂c/∂t = κ(∂²c/∂x² + ∂²c/∂y²)` の定常解、すなわち `∇²c=0` を求めます。κは定常解から消えるため、物理的な拡散速度や予測時間を指定する機能ではありません。移流・反応・潮汐・鉛直混合は含みません。

経緯度の規則格子を領域中央緯度の局所平面近似に変換し、4近傍の有限体積離散化で係数 `1/dx²`、`1/dy²` を使います。Numeric.js 1.2.6 の疎行列LU分解で解きます。長辺の格子数は48・64・96から選択でき、画面のリサイズによって計算格子は変化しません。

- 観測点を含む格子は測点期間平均を固定するDirichlet条件。同じ格子に複数測点がある場合は測点均等平均。
- 海岸線と外縁はNeumann流束ゼロ。セル中心とセル間の複数位置で陸域を判定し、陸域をまたぐ近傍接続を切断。
- 陸域に該当する観測点は補間から除外し、その件数を表示。勝手に海へ移動しません。
- 観測格子と連結しない海域は欠測。一つの固定値だけを持つ連結領域は一定値になります。
- 正規化残差は `|Σw(c_i-c_j)| / Σw`。許容値は `1e-7 * max(1, |固定値|)`。有限性と最大値原理も検査し、失敗した結果は出力しません。

国土地理院の海岸線は一般図用です。格子未満の水路・防波堤・埋立地を完全には解像しません。領域外縁の流束ゼロも解析上の仮定です。広域では局所平面近似の誤差が増えます。海洋モデルや不確かさ評価済みの客観解析の代わりではありません。観測密度・格子解像度・境界の妥当性を研究目的に合わせて確認してください。

## 密度

[python-seawater のEOS-80](https://pyoceans.github.io/python-seawater/eos80.html) のMIT実装を移植し、UNESCOの検証値でテストしています。塩分PSS-78、水温ITS-90を仮定します。実密度RHOは採水深度・緯度からSaunders式で推定した海圧を使用。RHO0は測定時水温・塩分で海圧0、σtはRHO0−1000です。ポテンシャル密度σ0・TEOS-10絶対塩分ではありません。適用範囲外の入力は密度を計算しません。

## 表示・保存

ポリゴン切り抜きはClipper 6.4.2.2（Boost License）を使用します。格子座標を100万倍した整数へ丸め、極小の重複線分によるSweepLineエラーを回避します。格子値・観測値は変更しません。穴・島の階層構造はPolyTreeから復元します。旧HTMLデモでは同じ処理を描画ピクセル座標で行います。

塗り分け、等値線、両方、観測点を選択可能。色範囲・等値線間隔を変更でき、色範囲外の値は端の色に飽和します。データそのものは切り捨てません。欠測境界を等値線と誤認しないよう、欠測に接する線分は表示しません。

NetCDFはCDF-1、CF-1.8形式。画面と同じ格子値、座標、陸域マスク、求解マスク、固定値、セル接続と解析メタデータを収録します。緯度は北から南へ降順。Shapefileは常に色分け帯のポリゴン（EPSG:4326）で、線表示を選んでも線ファイルではありません。ZIPのmetadata.jsonに表示形式と閾値を記録します。CSVは測点期間平均です。元の観測時系列すべての出力ではありません。

## 検証

`npm test`: 密度標準値、欠測・定量下限、年月平均、線形調和解、陸域分離・孤立海域、APIページング。

`node scripts/test-research.cjs`: 稼働中のサーバーに対し東京湾2010–2018年夏季の実水温・密度、表示範囲・種類、3形式保存、ソース別表示、スマホ幅を検証。既定URLは http://localhost:5175、`APP_URL` で変更できます。PlaywrightとEdgeが必要です。
